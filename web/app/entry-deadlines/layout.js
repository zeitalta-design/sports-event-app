export const metadata = {
  title: "エントリー締切カレンダー",
  description:
    "マラソン・ランニング大会のエントリー締切日を一覧で確認。締切間近の大会を見逃さない。",
};

export default function EntryDeadlinesLayout({ children }) {
  return children;
}
