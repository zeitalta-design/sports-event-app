export const metadata = {
  title: "大会カレンダー",
  description:
    "月別のスポーツ大会開催スケジュール。マラソン・ランニング大会の日程を一覧で確認。エントリー締切もチェック。",
};

export default function CalendarLayout({ children }) {
  return children;
}
