export const metadata = {
  title: "大会比較",
  description:
    "気になるマラソン・ランニング大会を並べて比較。日程・費用・距離・アクセスなどの違いが一目でわかります。",
  robots: { index: false },
};

export default function CompareLayout({ children }) {
  return children;
}
